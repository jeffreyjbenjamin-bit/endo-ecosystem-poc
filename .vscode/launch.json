{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run ingest (ctgov only, dry)",
      "type": "python",
      "request": "launch",
      "module": "src.ingest",
      "console": "integratedTerminal",
      "envFile": "${workspaceFolder}/.env",
      "justMyCode": true
    }
  ]
}